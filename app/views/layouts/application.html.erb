<!DOCTYPE html>
<html>
  <head>
    <title>Samplezon</title>
    <link rel="stylesheet" href="style.css"/>
    <link rel="stylesheet" href="http://content.codecademy.com/projects/bootstrap.min.css">
    <link href='http://fonts.googleapis.com/css?family=Roboto:100,300,400,700,500' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="header">
      <div class="container">
        <div class="navbar">

          <%# This is the logo at the top of the home page %>
            <a href="/"><img class="logo" src="https://i.imgur.com/susZNnd.png"/></a>

            <%# This is the searchbar at the top of the home page %>
              <form id="form" class="d-flex">
                <input type="search" class="form-control mr-sm-2" id="query" name="q" placeholder="Search...">
                <button class="btn btn-warning my-2 my-sm-0">Search</button>
              </form>

              <div class="nav pull-right">
                <% if current_user %>
                  <ul>
                    <li><%= link_to "Log out", logout_path, method: "delete", class: "btn btn-warning" %></li>
                  </ul>
                <% else %>
                  <ul class="d-flex">
                    <li><%= link_to "Signup", 'signup', class: "btn btn-warning" %></li>
                    <li><%= link_to "Login", 'login', class: "btn btn-warning" %></li>
                  </ul>
                <% end %>
              </div>
            </div>
          </div>

          <div class="main">
            <div class="container">
              <%# This is the link for login/signup %>

                <%= yield %>

              </div>
            </div>

            <div class="footer">
              <div class="container"></body>
            </html>
